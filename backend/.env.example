# Backend Environment Variables

# Server Configuration
NODE_ENV=development
PORT=4000

# Redis Configuration (Optional - will fallback to no caching if not available)
# Set DISABLE_REDIS=true to completely disable Redis connection attempts
DISABLE_REDIS=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Logging
LOG_LEVEL=debug

# Database Configuration
# Set to 'true' to use PostgreSQL database, 'false' for in-memory storage
USE_DATABASE=false
DATABASE_URL=postgresql://pokewave:pokewave_password@localhost:5432/pokewave_db

# JWT Secret (change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production